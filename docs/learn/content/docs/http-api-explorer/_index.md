---
title: "HTTP API Explorer"
description: "Manage HTTP API connections, invoke endpoints with runtime templates, inspect tabbed request/response history, and run regression collections — all from the browser."
weight: 13
---

The HTTP API Explorer extends MCP Explorer beyond MCP servers to cover plain HTTP endpoints. Define connections in Infrastructure, invoke them with runtime `{placeholder}` inputs, inspect full request/response history, and run regression collections against saved baselines.
