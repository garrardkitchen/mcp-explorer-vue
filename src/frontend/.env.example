# MCP Explorer Frontend — Environment Variables
# Copy to .env.local for local development

# Base URL for the API (leave empty when running behind the YARP gateway)
VITE_API_BASE_URL=http://localhost:5000

# Application version shown in the UI
VITE_APP_VERSION=0.5.0
